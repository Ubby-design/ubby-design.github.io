# Basic firewall rules for lab3

# Allow SSH (opsadmin only)
-A INPUT -p tcp --dport 22 -j ACCEPT
# Allow HTTP/HTTPS
-A INPUT -p tcp --dport 80 -j ACCEPT
-A INPUT -p tcp --dport 443 -j ACCEPT
# Drop all other traffic
-A INPUT -j DROP


Vagrant.configure("2") do |config|
  config.vm.box = "ubuntu/focal64"
  config.vm.hostname = "fsie-lab2"
  config.vm.network "private_network", type: "dhcp"

  config.vm.provision "shell", path: "provisioning/provision.sh"
end

